components:
  schemas:
    HealthStatusCheck:
      description: Health check Duplicate status
      type: object
      required:
        - status
        - timestamp
      properties:
        status:
          type: string
          enum:
            - SUCCESS
        timestamp:
          type: integer
          description: Number of milliseconds since Epoch time
          example: 11111123
